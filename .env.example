OPENAI_API_KEY=        # optional — only needed if using run_daily.py with OpenAI models
SLACK_BOT_TOKEN=       # your Slack bot token for daily cron posting
JQUANTS_API_KEY=       # J-Quants API v2 dashboard key (used by fetch_jp_market_data.py)
FRED_API_KEY=          # FRED API key (optional — used for Japanese-rate validation)
